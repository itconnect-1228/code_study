# TASK-085: 폴더 업로드로 태스크 생성 테스트

**작성일**: 2025년 12월 26일
**테스트 방법**: Playwright MCP 브라우저 자동화
**테스트 결과**: ✅ 통과

---

## 무엇을 테스트했나요?

사용자가 여러 파일이 포함된 폴더를 업로드할 때, 시스템이 폴더 구조(하위 디렉토리 포함)를 올바르게 보존하는지 확인했습니다.

---

## 왜 이 테스트가 중요한가요?

마치 서류 캐비넷에 폴더를 정리하는 것처럼, 프로그래밍 프로젝트도 폴더 구조로 파일을 정리합니다. 예를 들어:
- `models/` 폴더에는 데이터 관련 코드
- `utils/` 폴더에는 도움 함수들

이 구조가 업로드 후에도 유지되어야 사용자가 코드를 이해하기 쉽고, AI가 정확한 학습 문서를 생성할 수 있습니다.

---

## 테스트 과정

### 1단계: 테스트 폴더 구조 생성

```
test_folder/
├── main.py              # 메인 진입점
├── models/
│   ├── __init__.py      # 모델 패키지
│   └── user.py          # User 클래스
└── utils/
    ├── __init__.py      # 유틸리티 패키지
    └── helpers.py       # 헬퍼 함수들
```

### 2단계: 기존 계정으로 로그인
- 이메일: `task84_test@example.com`
- 로그인 성공 확인

### 3단계: 프로젝트 접속 및 태스크 생성
- 프로젝트: "TASK 84 테스트 프로젝트"
- "새 태스크" 클릭 → "폴더" 탭 선택
- 태스크 제목: "Python 프로젝트 구조 학습"
- 폴더 선택: `test_folder/` (5개 파일)

### 4단계: 결과 검증

**API 응답 확인**:
```json
{
  "id": "57e24c27-c398-48eb-bc6c-8169b7635623",
  "task_number": 2,
  "title": "Python 프로젝트 구조 학습",
  "upload_method": "folder",
  "deletion_status": "active"
}
```

**데이터베이스 파일 경로 검증**:
| 원본 파일 | 저장된 경로 | 결과 |
|----------|-----------|------|
| main.py | test_folder/main.py | ✅ |
| models/__init__.py | test_folder/models/__init__.py | ✅ |
| models/user.py | test_folder/models/user.py | ✅ |
| utils/__init__.py | test_folder/utils/__init__.py | ✅ |
| utils/helpers.py | test_folder/utils/helpers.py | ✅ |

---

## 테스트 결과 요약

| 검증 항목 | 기대값 | 실제값 | 결과 |
|-----------|--------|--------|------|
| 태스크 생성 | 201 Created | 201 Created | ✅ |
| upload_method | "folder" | "folder" | ✅ |
| 파일 수 | 5개 | 5개 | ✅ |
| 폴더 구조 보존 | 유지 | 유지 | ✅ |
| 하위 폴더 경로 | 포함 | 포함 | ✅ |

---

## 발견된 이슈

### 백엔드 API 버그: GET /tasks/{task_id}/code
- **증상**: 500 Internal Server Error
- **원인**: SQLAlchemy lazy loading이 async 컨텍스트 외부에서 실행됨
- **에러 메시지**: `MissingGreenlet: greenlet_spawn has not been called`
- **영향**: 코드 파일 조회 API가 작동하지 않음
- **권장 조치**: `selectinload()` 또는 `joinedload()`를 사용하여 eager loading으로 수정

### 프론트엔드 렌더링 이슈
- TASK 84에서 발견된 동일한 버그
- 태스크 목록이 UI에 표시되지 않음

---

## 테스트에 사용된 파일

### main.py
```python
# Main entry point
from models.user import User
from utils.helpers import greet

if __name__ == "__main__":
    user = User("홍길동", "hong@example.com")
    print(greet(user.name))
```

### models/user.py
```python
# User model
class User:
    def __init__(self, name: str, email: str):
        self.name = name
        self.email = email
```

### utils/helpers.py
```python
# Helper functions
def greet(name: str) -> str:
    return f"안녕하세요, {name}님!"
```

---

## 관련 파일

- 백엔드 태스크 API: [backend/src/api/tasks.py](../../backend/src/api/tasks.py)
- 코드 업로드 서비스: [backend/src/services/code_analysis/code_upload_service.py](../../backend/src/services/code_analysis/code_upload_service.py)
- 파일 저장 서비스: [backend/src/services/code_analysis/file_storage.py](../../backend/src/services/code_analysis/file_storage.py)

---

## 다음 단계

1. **T086**: 붙여넣기 업로드 테스트 - 언어 자동 감지 확인
2. **T087**: 11MB 파일 업로드 거부 테스트
3. **T088**: 바이너리 파일 업로드 거부 테스트
4. **백엔드 버그 수정**: GET /tasks/{task_id}/code API 수정

---

## TDD 사이클 기록

- 🔴 **RED**: 폴더 업로드 및 구조 보존 테스트 케이스 정의
- 🟢 **GREEN**: 폴더 구조가 file_path 필드에 올바르게 저장됨 확인
- 🔵 **REFACTOR**: 코드 파일 조회 API 버그 발견 - 별도 이슈로 처리 필요
